strategy: "plugin_merge"
refresh_frequency: 1

layouts:
  - full
  - half_horizontal
  - half_vertical
  - quadrant

name: "Calendar XL"
description: "Same old calendar, but XL. NOW front and center, NEXT at a glance, LATER in the wings — readable from across the room."
author: "Hossain Khan"
version: "1.0.0"
tap_action_url: ""

# Current implementation note:
# Calendar XL derives NOW, NEXT, and LATER directly from TRMNL Plugin Merge data
# supplied by a native calendar plugin. The Liquid templates expect a namespaced
# merge root such as google_calendar_29713.
