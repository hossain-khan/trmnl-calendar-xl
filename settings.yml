strategy: "polling"
polling_url: "https://your-calendar-api.example.com/trmnl/agenda"
refresh_frequency: 1

layouts:
  - full
  - half_horizontal
  - half_vertical
  - quadrant

name: "Calendar XL"
description: "A glanceable Now / Next / Later agenda optimized for TRMNL displays."
author: "Hossain Khan"
version: "1.0.0"
tap_action_url: ""

# Current implementation note:
# Calendar XL derives NOW, NEXT, and LATER directly from TRMNL Plugin Merge data
# supplied by a native calendar plugin. The Liquid templates expect a namespaced
# merge root such as google_calendar_29713.
