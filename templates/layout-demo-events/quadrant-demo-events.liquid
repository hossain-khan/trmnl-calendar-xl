{% comment %}
  DEMO: quadrant — active NOW event state (falls back to NEXT if nothing happening now).
  Single hero panel fills entire layout — no NEXT/LATER sections.
  All HTML inlined (no render calls). Static data for responsive testing.

  Devices tested:
    TRMNL OG  (md, 400×240 effective) → full height ~200px → value budget ~130px → fit_height 130
    TRMNL X   (lg, 520×390 effective) → full height ~350px → value budget ~280px → fit_height 280

  Two-span strategy for responsive hero title:
    block lg:hidden  → visible on sm/md (TRMNL OG), hidden on lg+
    hidden lg:block  → hidden on sm/md, visible on lg+ (TRMNL X)

  Note: In production, label shows "NOW" when an event is active,
  or "NEXT" when the user is free and the next upcoming event is promoted.
{% endcomment %}

<div class="layout">
  <div class="rounded--large p--2 flex flex--col gap--small h--full text--white bg--black">

    {% comment %} Label row: section tag left, event time right {% endcomment %}
    <div class="flex flex--row flex--between flex--center-y gap--xsmall">
      <span class="label lg:label--large label--inverted">NOW</span>
      <span class="label lg:label--large label--inverted">2:15 PM – 3:15 PM</span>
    </div>

    {% comment %} Hero title — sm/md (TRMNL OG quadrant ≈ 400×240) {% endcomment %}
    <span class="value value--xxxlarge text--center block lg:hidden"
          data-value-fit="true"
          data-value-fit-max-height="130">MEET SAMANTHA AND EMILY AT DMS</span>

    {% comment %} Hero title — lg (TRMNL X quadrant ≈ 520×390) {% endcomment %}
    <span class="value value--xxxlarge text--center hidden lg:block"
          data-value-fit="true"
          data-value-fit-max-height="260">MEET SAMANTHA AND EMILY AT THE SCHOOL AND THEN MEET UP LATER</span>

  </div>
</div>

<div class="title_bar">
  <span class="title">Calendar XL</span>
  <span class="instance">DEMO · NOW ACTIVE</span>
</div>
